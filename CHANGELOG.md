# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

## [0.1.2] — 2026-08-29

### 고쳤습니다

- **Fire Bar 아이콘으로 연 메뉴가 1.5초 뒤 저절로 닫히던 문제를 고쳤습니다.**
  범인은 Fire 자신의 폴백 클릭이었습니다. 메뉴가 붙은 status item의 `AXPress`는
  메뉴를 즉시 열지만 **성공을 반환하지 못합니다** — 열린 메뉴의 트래킹이 응답을
  막고 있다가 ~1.5초 뒤 `-25204(CannotComplete)`로 끝납니다. Fire는 이걸 실패로
  해석해 좌표 클릭 폴백을 쐈고, 그 합성 클릭이 방금 연 메뉴를 토글로 닫았습니다.
  (Fire를 죽이면 폴백이 안 나가서 메뉴가 살아남던 단서와 정확히 일치합니다.)
  이제 `AXPress`를 백그라운드에서 발사하고 성패를 반환값이 아니라 **화면 관찰**
  (팝업 메뉴 창 등장)로 가립니다. 메뉴가 열렸으면 폴백을 쏘지 않고, 창이 닫힌 것을
  확인한 뒤에 숨김을 복원합니다. 메뉴는 바깥 클릭으로 닫힐 때까지 유지됩니다.
- **합성 클릭(postToPid)으로 연 메뉴는 macOS가 ~0.27초 뒤 거둬간다는 것을
  실측으로 확인했습니다.** 접근성이 통하지 않는 항목에만 쓰는 최후 수단으로
  내렸습니다.
- **메뉴가 열려 있는 동안 열린 메뉴의 소유 앱에 접근성 질의를 보내지 않습니다.**
  닫힘 감지는 CGWindowList의 팝업 창 관찰로만 합니다.
- **이미 화면에 있는 항목은 클릭 전 재스캔을 건너뜁니다.** 전체 재스캔의 접근성
  순회가 1.2초를 잡아먹어 클릭 반응이 그만큼 늦었습니다.
- **재빌드 뒤 "시스템 설정 토글은 켜져 있는데 권한이 없는" 상태의 출구를
  만들었습니다.** 서명이 바뀐 빌드를 macOS가 다른 앱으로 취급하면 낡은 TCC 기록이
  남습니다. 설정 화면 권한 행에 "기록 초기화" 버튼을 달아 `tccutil reset` 뒤
  정직한 꺼짐 상태에서 다시 켤 수 있게 했습니다.

### 바꿨습니다

- **영어를 지원합니다.** 시스템 언어가 한국어면 한국어, 아니면 영어로 표시됩니다.
- 설정 화면의 "메뉴바에 표시" 구역 설명을 정직하게 바꿨습니다 — 다른 앱 아이콘은
  메뉴바에서 ⌘+드래그로 직접 옮겨야 하고, 여기서는 Fire 아이콘만 옮겨진다고
  명시합니다. 적용되지 않은 항목의 안내문에도 ⌘+드래그 방법을 함께 적습니다.

### 이전 항목

- **Fire Bar 아이콘을 눌러도 반응이 없던 문제를 고쳤습니다.** 원인이 세 겹이었습니다.
  - 숨긴 항목에도 접근성 `AXPress`가 성공합니다. 다만 메뉴가 그 항목이 있는 화면 밖
    좌표에 열립니다. 성공을 반환하기 때문에 다음 경로로 넘어가지도 않았습니다.
    측정해 보니 메뉴가 `149x176` 크기로 `x=-995`에 떠 있었습니다.
  - 창이 없는 항목(`windowNumber == 0`)은 접근성이 알려주는 좌표를 믿을 수 없습니다.
    화면이 1470pt인데 `x=2643`으로 보고합니다. 그래서 "화면 안인가" 판정이 통과해
    버렸습니다. 실제 창이 있을 때만 이 지름길을 쓰도록 했습니다.
  - 메뉴가 떴는지 0.7초에 한 번만 확인했습니다. 그 뒤에 뜬 메뉴를 놓치고 실패로
    처리했고, 실패 처리가 숨김을 되돌리면서 방금 뜬 메뉴를 닫았습니다.
    이제 조건이 만족될 때까지 기다립니다.
- **Fire Bar 패널을 닫는 순서를 바꿨습니다.** 예전에는 메뉴가 열린 뒤에 닫았는데,
  `orderOut`이 포커스를 옮기고 status item 메뉴는 포커스를 잃으면 사라집니다.
  이제 패널을 먼저 닫고 누릅니다.


## [0.1.1] — 2026-08-28

### 더했습니다

- **자동 업데이트(Sparkle).** 새 버전이 나오면 앱이 알려드립니다. 설정에서 끄실 수 있습니다.
  업데이트 피드는 `rrllab.com`에 둡니다. 이 주소는 배포된 앱 안에 영구히 박히고
  이미 설치된 앱이 몇 년 뒤에도 계속 조회하기 때문에, 호스팅을 바꿀 수 있는
  자기 도메인이어야 합니다.
- **앱 아이콘.** 응용 프로그램과 Spotlight에서 아이콘이 표시됩니다.
- **배포 파이프라인.** Developer ID 서명부터 공증, dmg, GitHub 릴리즈까지 한 줄로
  이어집니다. 자세한 내용은 [docs/RELEASING.md](docs/RELEASING.md)에 있습니다.

### 바꿨습니다

- 빌드 산출물 위치를 `build/`에서 `.build/app/`으로 옮겼습니다. `build/`는 스팟라이트가
  색인해서, 응용 프로그램 검색에 빌드 산출물이 설치본과 나란히 떴습니다.
  숨김 폴더는 색인하지 않습니다.
- README를 영어 기본으로 바꾸고 한국어는 `README.ko.md`로 분리했습니다.

## [0.1.0]

첫 동작본입니다. 자세한 이력은 [HANDOFF.md](HANDOFF.md)에 있습니다.

- 메뉴바 아이콘을 메인 메뉴바와 Fire Bar 두 구역으로 나눕니다
- 적용한 뒤 메뉴바를 다시 측정해 결과를 확인하고, 어긋나면 재시도합니다
- 실패 방향을 구분합니다. "보여야 할 것이 사라짐"만 위험으로 보고 펼칩니다
- Fire Bar 패널, 클릭 프록시, 빈 메뉴바 클릭, Watchdog

[미출시]: https://github.com/kimhung910924/fire-menubar/compare/v0.1.1...HEAD
[0.1.1]: https://github.com/kimhung910924/fire-menubar/releases/tag/v0.1.1
