# Fire 기획안

> 내부 프로젝트명: **Fire**  
> 플랫폼: macOS 전용  
> 앱 형태: Dock에 나타나지 않는 메뉴바 유틸리티  
> 핵심 목표: 메뉴바 아이콘을 **메인 메뉴바**와 **Fire Bar** 두 구역으로 나누고, 외부 모니터·잠자기·재부팅 이후에도 안정적으로 복구한다.

---

## 1. 프로젝트 개요

**Fire**는 복잡해진 macOS 메뉴바 아이콘을 두 구역으로 정리하는 초경량 메뉴바 앱이다.

사용자는 자주 보는 아이콘만 메인 메뉴바에 남기고, 나머지 아이콘은 메뉴바 아래에 열리는 별도 막대인 **Fire Bar**에 보관한다.

Fire Bar는 다음 방법으로 연다.

- 메뉴바의 비어 있는 영역 클릭
- 전역 단축키
- 필요할 경우 설정 화면의 테스트 버튼

Fire Bar 안의 아이콘은 실제 메뉴바 아이콘처럼 클릭할 수 있어야 하며, 드래그로 순서를 바꿀 수 있어야 한다.

기능을 많이 추가하지 않는다. 다음 네 가지에 집중한다.

1. 메인 메뉴바와 Fire Bar로 아이콘 분류
2. 설정 화면에서 드래그로 분류 및 순서 변경
3. 메뉴바 빈 공간 클릭 또는 단축키로 Fire Bar 열기
4. 외부 모니터 연결·해제와 잠자기 복귀 후 자동 복구

---

## 2. 제품 목표

### 핵심 제품 정의

> 메뉴바 아이콘을 메인 메뉴바와 별도 Fire Bar로 나누고, 시스템 상태가 바뀌어도 사용자가 다시 설정하지 않도록 자동 복구하는 앱

### 최우선 가치

Fire의 우선순위는 다음 순서로 둔다.

1. 안정성
2. 정확한 아이콘 클릭 동작
3. 외부 모니터 및 잠자기 복구
4. 단순한 설정
5. 시각적 완성도

색상 꾸미기, 애니메이션, 검색, 프로필 같은 기능보다 아이콘이 사라지거나 배치가 풀리지 않는 것이 더 중요하다.

---

## 3. 앱 이름과 아이콘

### 앱 이름

- 내부 프로젝트명: `Fire`
- Xcode 프로젝트명: `Fire`
- 표시명: `Fire`
- Bundle Identifier 예시: `com.rrllab.FireMenuBar`

### 아이콘

불꽃 모양의 단순한 템플릿 아이콘을 사용한다.

요구사항:

- macOS 메뉴바의 라이트·다크 모드에 맞게 자동 반전
- 작은 크기에서도 형태가 명확한 단색 불꽃
- 컬러 이모지 형태가 아닌 `NSImage` 템플릿 아이콘
- 앱 아이콘은 빨강·주황 계열 불꽃 사용 가능
- 메뉴바 아이콘은 시스템 스타일의 단색 아이콘 사용

### 이름 관련 주의

`Fire`라는 이름의 Ice 기반 공개 프로젝트가 이미 존재하므로, 개인용 내부 프로젝트에서는 그대로 사용할 수 있지만 외부 공개·배포 전에는 혼동 방지를 위해 이름을 다시 검토한다.

---

## 4. 지원 환경

### 권장 범위

- macOS 14 Sonoma 이상
- Apple Silicon 우선
- Intel Mac은 후순위 검증
- 노치가 있는 MacBook과 노치가 없는 Mac 모두 지원
- 외부 모니터 1대 이상 지원
- 확장 화면과 미러링 모드 검증

### 앱 형태

- Swift
- AppKit 중심
- 일부 설정 UI만 SwiftUI 사용 가능
- Electron 또는 Tauri 사용하지 않음
- Dock 아이콘 없음
- 일반 앱 전환 목록에 나타나지 않는 accessory 앱
- 네트워크 연결 불필요
- 계정과 서버 없음
- 분석 및 추적 없음

---

## 5. 최종 기능 범위

### 포함 기능

- 메뉴바 아이콘 자동 탐색
- 메인 메뉴바와 Fire Bar 두 구역
- 설정 화면에서 아이콘 드래그 이동
- 각 구역 안에서 드래그로 순서 변경
- 메뉴바 왼쪽과 오른쪽의 실제 빈 영역 클릭 감지
- 클릭한 화면에 Fire Bar 표시
- Fire Bar 바깥 클릭 시 닫기
- 일정 시간 사용하지 않으면 자동 닫기
- `Esc`로 닫기
- 전역 단축키로 Fire Bar 열기·닫기
- 전역 단축키로 설정창 열기
- Fire 자체 아이콘도 메인 메뉴바 또는 Fire Bar에 배치
- Fire 아이콘 클릭 시 설정창 열기
- 로그인 시 자동 실행
- 외부 모니터 연결·해제 후 자동 복구
- 주 디스플레이 변경 후 자동 복구
- 해상도·배율·화면 배열 변경 후 자동 복구
- 잠자기·화면 절전 복귀 후 자동 복구
- 메뉴바 아이콘이 새로 생기거나 사라질 때 재탐색
- 수동 상태 복구 버튼
- 권한 상태 확인 및 다시 요청 안내

### 제외 기능

초기 버전에는 다음을 넣지 않는다.

- 메뉴바 색상 변경
- 메뉴바 투명도 및 그라데이션
- 메뉴바 모양 변경
- 아이콘 검색
- 세 번째 `항상 숨김` 구역
- 호버로 열기
- 스크롤 또는 스와이프로 열기
- 여러 레이아웃 프로필
- 앱별 조건부 표시
- 위젯
- AI 기능
- 자동 업데이트
- 클라우드 동기화
- 통계 및 분석
- 메뉴바 아이콘 간격 조절
- 여러 종류의 Fire Bar 테마

---

## 6. 기본 사용자 흐름

### 최초 실행

1. Fire를 실행한다.
2. Dock에는 아이콘이 나타나지 않는다.
3. 필요한 시스템 권한을 안내한다.
4. Fire가 현재 메뉴바 아이콘을 탐색한다.
5. 모든 아이콘을 기본적으로 메인 메뉴바 구역에 둔다.
6. Fire 불꽃 아이콘이 메뉴바에 표시된다.
7. 불꽃 아이콘을 누르면 설정창이 열린다.
8. 사용자가 아이콘을 Fire Bar 구역으로 드래그한다.
9. 설정이 즉시 실제 메뉴바에 반영된다.

### 평상시 사용

1. 메뉴바의 빈 영역을 클릭한다.
2. 클릭한 모니터의 메뉴바 바로 아래에 Fire Bar가 열린다.
3. Fire Bar 안의 아이콘을 클릭해 해당 앱 메뉴를 사용한다.
4. 바깥을 클릭하거나 `Esc`를 누르면 닫힌다.
5. 아무 조작이 없으면 일정 시간 후 자동으로 닫힌다.

---

## 7. 메뉴바 빈 영역 클릭

### 왼쪽과 오른쪽 모두 지원

Fire는 메뉴바 오른쪽뿐 아니라 **왼쪽의 실제 빈 공간도 클릭해서 열 수 있게 한다.**

단, macOS의 Apple 메뉴, 현재 앱 메뉴, 시스템 상태 아이콘 또는 다른 메뉴바 아이콘의 클릭을 가로채면 안 된다.

따라서 단순히 메뉴바 전체에 투명 창을 덮지 않는다.

### 빈 영역 판정

클릭 지점이 다음 요소에 속하지 않을 때만 빈 영역으로 판정한다.

- Apple 메뉴
- 현재 앱 이름 메뉴
- 파일·편집·보기 등 앱 메뉴
- 노치 또는 카메라 영역
- 시스템 상태 아이콘
- 서드파티 메뉴바 아이콘
- Fire 자체 아이콘
- 열려 있는 메뉴 또는 팝오버

판정 방식:

1. 현재 클릭한 디스플레이 확인
2. 메뉴바 프레임 확인
3. 접근성 API로 현재 메뉴 및 메뉴바 아이콘의 실제 프레임 수집
4. 클릭 좌표가 어떤 항목 프레임에도 포함되지 않는지 확인
5. 빈 영역일 때만 Fire Bar 토글

### 클릭 동작

```text
Fire Bar가 닫혀 있음
→ 빈 영역 클릭
→ Fire Bar 열기

Fire Bar가 열려 있음
→ 빈 영역 클릭
→ Fire Bar 닫기
```

### 클릭 위치와 Fire Bar 위치

Fire Bar는 클릭한 지점 아래에 표시한다.

- 클릭한 X 좌표를 기준으로 배치
- 화면 왼쪽·오른쪽 경계를 넘어가지 않도록 자동 보정
- Fire Bar가 넓으면 클릭 지점을 중심으로 배치
- 노치가 있어도 화면 가용 영역 안에 배치
- 클릭한 모니터에 표시
- 외부 모니터에서 클릭하면 외부 모니터에 표시

---

## 8. Fire Bar 동작

### 기본 형태

Fire Bar는 메뉴바 바로 아래에 뜨는 둥근 직사각형 패널이다.

```text
┌────────────────────────────────────────┐
│  [아이콘] [아이콘] [아이콘] [아이콘]  │
└────────────────────────────────────────┘
```

### 외형

- macOS 시스템 팝오버와 유사한 반투명 재질
- 밝은 모드와 어두운 모드 자동 대응
- 지나치게 강한 색상 없음
- 모서리 둥글게
- 약한 그림자
- 아이콘 크기와 간격 고정
- 내용에 맞춰 가로 길이 자동 조절
- 화면 너비를 넘으면 2줄 대신 가로 스크롤 사용 검토
- 1차 버전에서는 한 줄 표시 우선

### 열기

다음 방법으로 연다.

- 메뉴바 왼쪽 빈 영역 클릭
- 메뉴바 오른쪽 빈 영역 클릭
- `⌥⌘F` 전역 단축키

단축키로 열 때 표시 화면:

1. 마우스 포인터가 있는 화면
2. 포인터 화면을 찾지 못하면 현재 활성 화면
3. 그마저 불명확하면 내장 또는 주 디스플레이

### 닫기

다음 경우 닫는다.

- Fire Bar 바깥 클릭
- 메뉴바 빈 영역 다시 클릭
- `Esc`
- `⌥⌘F` 다시 누름
- 자동 닫기 타이머 만료
- 표시 중인 모니터가 분리됨
- 사용자 세션이 잠김
- 앱 종료

### 자동 닫기

기본값은 **5초**로 한다.

다음 동작이 있으면 타이머를 다시 시작한다.

- Fire Bar 위로 마우스 이동
- 아이콘 클릭
- 드래그 시작 또는 종료
- Fire Bar 내부 스크롤
- 아이콘 메뉴 사용

다음 상태에서는 자동으로 닫지 않는다.

- 마우스가 Fire Bar 위에 있음
- 드래그 중
- Fire Bar 아이콘으로 연 메뉴가 화면에 열려 있음
- 설정창에서 레이아웃을 편집 중

자동 닫기 시간은 초기 설정 화면에 노출하지 않고 고정값으로 둔다. 실제 사용 후 필요하면 나중에 옵션화한다.

---

## 9. Fire Bar 아이콘 클릭

Fire Bar에 보이는 아이콘은 단순 이미지가 아니라 실제 메뉴바 항목을 조작하는 프록시다.

### 기본 동작

1. 사용자가 Fire Bar의 아이콘을 클릭한다.
2. Fire가 해당 아이콘의 원본 메뉴바 항목을 식별한다.
3. 접근성 액션으로 원본 항목의 클릭을 실행한다.
4. 해당 앱의 메뉴 또는 팝오버가 정상적으로 열린다.
5. 메뉴가 닫힌 뒤 Fire Bar 자동 닫기 타이머를 다시 시작한다.

### 구현 우선순위

1. 접근성 API의 Press 액션
2. 원본 아이콘의 실제 좌표 클릭
3. 필요한 경우 원본 항목을 임시로 표시한 후 클릭
4. 작업 완료 후 다시 숨김 상태 복원

### 기술 검증 필수 항목

일부 메뉴바 앱은 일반적인 접근성 Press 액션을 지원하지 않거나, 메뉴가 원래 아이콘 위치에 열릴 수 있다.

따라서 본 개발 전에 다음을 실제로 검증해야 한다.

- Google Drive
- Dropbox 또는 유사 동기화 앱
- VPN 앱
- 클립보드 앱
- 배터리 또는 전원 관련 앱
- 입력기
- macOS 제어 센터 항목
- Fire 자체 아이콘

클릭했을 때 메뉴가 Fire Bar 아이콘 아래에 정확히 뜨지 않더라도 기능 사용이 가능하면 1차 버전에서는 허용할 수 있다. 다만 화면 밖이나 엉뚱한 모니터에 열리는 것은 실패로 본다.

---

## 10. 설정 화면

Fire 아이콘을 클릭하면 설정창 하나만 열린다.

일반 팝오버 메뉴는 만들지 않는다.

### 설정 화면 예시

```text
Fire

메뉴바에 표시
┌──────────────────────────────────────────────────────┐
│ [Wi-Fi] [배터리] [한/영] [Fire] [기타 아이콘]       │
└──────────────────────────────────────────────────────┘

Fire Bar에 표시
┌──────────────────────────────────────────────────────┐
│ [Drive] [VPN] [클립보드] [기타 아이콘]              │
└──────────────────────────────────────────────────────┘

☑ 로그인 시 자동 실행

단축키
Fire Bar 열기/닫기    ⌥⌘F
설정 열기             ⌥⌘,

[아이콘 다시 검색] [메뉴바 상태 다시 적용]
```

### 아이콘 배치

사용자는 다음 동작을 할 수 있다.

- 같은 구역 안에서 좌우 드래그로 순서 변경
- 메인 메뉴바에서 Fire Bar로 드래그
- Fire Bar에서 메인 메뉴바로 드래그
- Fire 자체 아이콘도 동일하게 드래그
- 드래그 완료 즉시 적용
- 적용 실패 시 원래 위치로 복원하고 오류 표시

### Fire 아이콘 위치

Fire 아이콘도 다음 두 위치 중 하나를 선택할 수 있다.

```text
메인 메뉴바
Fire Bar
```

Fire 아이콘이 Fire Bar에 들어간 경우:

- 빈 메뉴바 클릭으로 Fire Bar를 연다.
- `⌥⌘F`로 Fire Bar를 연다.
- Fire Bar 안의 불꽃 아이콘을 눌러 설정창을 연다.
- `⌥⌘,`로 설정창을 직접 열 수 있다.

따라서 Fire 아이콘을 메인 메뉴바에서 숨겨도 설정 접근 경로가 남는다.

---

## 11. 단축키

### 기본 단축키

```text
⌥⌘F     Fire Bar 열기 / 닫기
⌥⌘,     Fire 설정 열기
Esc      Fire Bar 닫기
```

### 단축키 동작 원칙

- 다른 앱의 일반 단축키를 최대한 침범하지 않는 조합 사용
- 전역 단축키 등록 실패 시 설정 화면에 경고
- 사용자가 다른 앱과 충돌할 경우 추후 변경 기능 추가 가능
- 1차 버전은 기본 단축키 고정으로 시작 가능
- 설정창이 열려 있어도 `⌥⌘F`는 Fire Bar를 토글
- Fire가 백그라운드 상태여도 동작

---

## 12. 로그인 시 자동 실행

설정 화면에 다음 체크박스만 둔다.

```text
☑ 로그인 시 자동 실행
```

### 기본값

최초 권한 설정을 마친 뒤 기본적으로 켜도록 제안하되, 사용자가 끌 수 있다.

### 구현

- macOS 13 이상 `SMAppService` 사용
- 별도 Helper 앱을 가능한 한 만들지 않음
- 로그인 후 Dock 없이 백그라운드 실행
- 시작 직후 저장된 화면 참조를 복원하지 않고 현재 상태를 새로 탐색
- 초기 메뉴바 탐색 실패 시 지연 재시도

---

## 13. 권한

Fire는 다른 앱의 메뉴바 항목을 읽고 조작해야 하므로 시스템 권한이 필요하다.

### 필수 권한

#### 손쉬운 사용

사용 목적:

- 메뉴바 항목 탐색
- 항목 프레임 확인
- 클릭 액션 실행
- 빈 메뉴바 영역 판정
- 아이콘 순서와 상태 관리

#### 화면 및 시스템 오디오 기록 또는 화면 기록

사용 목적:

- Fire Bar와 설정 화면에 실제 메뉴바 아이콘 이미지를 정확하게 표시
- 일부 메뉴바 항목 이미지 캡처
- 운영체제 버전에 따라 필요한 메뉴바 이미지 정보 확보

Fire는 화면 내용을 저장하거나 네트워크로 전송하지 않는다.

### 권한 화면

최초 실행 시 다음 순서로 안내한다.

1. 손쉬운 사용 권한 설명
2. 시스템 설정 열기
3. 권한 승인 여부 실시간 확인
4. 화면 기록 권한 설명
5. 시스템 설정 열기
6. 필요하면 Fire 재실행
7. 아이콘 탐색 시작

권한이 없을 때 조용히 실패하지 않는다. 설정 화면에 현재 권한 상태와 해결 방법을 표시한다.

---

## 14. 아이콘 분류 모델

Fire는 아이콘을 두 구역으로 관리한다.

```text
MAIN
FIRE_BAR
```

세 번째 `항상 숨김` 구역은 만들지 않는다.

### 저장 데이터

각 아이콘마다 다음 정보를 저장한다.

```json
{
  "stableId": "derived-stable-id",
  "ownerBundleId": "com.example.app",
  "ownerName": "Example",
  "accessibilityTitle": "Example Menu",
  "section": "FIRE_BAR",
  "order": 3,
  "lastSeenAt": "2026-07-31T23:30:00+09:00",
  "isFireControlItem": false
}
```

### 안정적인 식별자 원칙

다음 값 하나만 믿지 않는다.

- PID
- 메뉴바 윈도우 번호
- 디스플레이 번호
- 현재 화면 좌표
- 아이콘 이미지 해시

PID와 윈도우 번호는 앱 재실행, 잠자기, macOS 업데이트 후 바뀔 수 있다.

따라서 다음 신호를 조합해 아이콘을 식별한다.

- 소유 앱 Bundle Identifier
- 접근성 역할과 제목
- 아이콘의 내부 태그 또는 시스템 식별자
- 메뉴바 내 상대 순서
- 이미지 특성
- 이전 탐색 결과
- 제어 센터 소유 항목에 대한 별도 매핑

식별이 불확실하면 자동으로 잘못 배치하지 않고 `새 아이콘`으로 표시해 사용자가 확인하게 한다.

---

## 15. 안정성 설계

Fire의 핵심은 외부 모니터나 잠자기 이후에도 이전 객체를 계속 붙잡지 않는 것이다.

### 기본 원칙

> 시스템 구성이 바뀌면 기존 화면·윈도우·아이콘 참조를 버리고 현재 상태를 다시 탐색한다.

### 전체 재구성 과정

```text
1. 열려 있는 Fire Bar 닫기
2. 진행 중인 드래그와 임시 클릭 작업 취소
3. 기존 메뉴바 항목 참조 폐기
4. 현재 연결된 디스플레이 다시 조회
5. 현재 메뉴바 항목 다시 탐색
6. 저장된 stableId와 새 항목 매칭
7. MAIN / FIRE_BAR 구역 다시 적용
8. 적용 결과 검증
9. 실패한 항목만 지연 재시도
```

### 직렬 실행

재구성 작업은 동시에 두 번 실행하지 않는다.

- 단일 상태 머신
- 단일 직렬 작업 큐
- 중복 이벤트 debounce
- 실행 중 새 이벤트가 오면 마지막에 한 번 더 재구성
- 중간 상태가 파일에 저장되지 않도록 원자적 적용

---

## 16. 외부 모니터 복구

### 감지 대상

- 외부 모니터 연결
- 외부 모니터 분리
- USB-C 허브 재연결
- HDMI 또는 DisplayPort 재연결
- 주 디스플레이 변경
- 디스플레이 배열 변경
- 해상도 변경
- 배율 변경
- 미러링과 확장 모드 전환
- 클램셸 모드 진입
- 클램셸 모드 해제

### 복구 방식

디스플레이 변경 이벤트가 발생하면 한 번만 처리하지 않는다.

```text
즉시 상태 정리
0.25초 후 1차 탐색
1초 후 2차 검증
3초 후 최종 검증
```

macOS가 모니터 연결 직후 디스플레이와 메뉴바 상태를 여러 단계로 갱신할 수 있으므로 지연 검증을 둔다.

### Fire Bar 표시 화면

- 메뉴바를 클릭한 화면에 표시
- 단축키 사용 시 포인터가 있는 화면에 표시
- Fire Bar가 열린 상태에서 해당 화면이 분리되면 즉시 닫기
- 남은 화면을 다시 탐색하고 정상 상태 재구성
- 디스플레이 ID를 영구 설정값으로 저장하지 않음

### 설정 공유

아이콘의 MAIN / FIRE_BAR 분류와 순서는 모든 모니터에서 공통으로 사용한다.

화면별로 별도의 프로필은 만들지 않는다.

---

## 17. 잠자기 및 세션 복구

### 감지 대상

- Mac 잠자기
- 화면만 절전
- 잠자기 복귀
- 화면 절전 복귀
- 사용자 세션 잠금
- 사용자 세션 재활성화
- Fast User Switching
- 앱이 장시간 백그라운드에 있다가 다시 활성화됨

### 복귀 처리

1. Fire Bar 강제 닫기
2. 기존 AXUIElement 및 윈도우 참조 폐기
3. 0.5초 대기
4. 디스플레이와 메뉴바 항목 재탐색
5. 저장된 분류 다시 적용
6. 2초 후 결과 검증
7. 실패 시 5초 후 마지막 재시도

잠자기 전의 PID, 윈도우 번호 또는 화면 번호를 그대로 사용하지 않는다.

---

## 18. Watchdog

macOS 이벤트를 놓치거나 특정 앱이 메뉴바 항목을 다시 만든 경우를 대비해 가벼운 상태 감시를 둔다.

### 주기

기본 15초

### 확인 항목

- 현재 디스플레이 수
- 현재 메뉴바 항목 수
- Fire 제어 아이템 존재 여부
- MAIN / FIRE_BAR 구역 상태
- 저장된 아이콘과 실제 아이콘 매칭 상태
- Fire Bar가 유효하지 않은 화면에 떠 있는지 여부
- 권한이 중간에 해제되었는지 여부
- 이벤트 탭이 정상 작동하는지 여부

### 복구

이상이 있으면 즉시 전체 재구성하지 않고 다음 순서로 처리한다.

1. 가벼운 재검사
2. 불일치가 계속되면 재구성
3. 연속 실패하면 설정 화면에 상태 표시
4. 사용자가 `메뉴바 상태 다시 적용` 실행 가능

Watchdog은 CPU를 거의 사용하지 않도록 단순 상태 비교만 수행한다.

---

## 19. 빈 클릭 및 전역 이벤트 처리

메뉴바 빈 영역 클릭을 잡기 위해 전역 마우스 이벤트를 감지한다.

### 요구사항

- Apple 메뉴와 앱 메뉴 클릭을 방해하지 않음
- 다른 메뉴바 아이콘 클릭을 방해하지 않음
- 클릭 이벤트를 무조건 소비하지 않음
- 실제 빈 영역으로 판정된 경우에만 Fire가 반응
- 외부 모니터 메뉴바에서도 동작
- 전체화면에서 메뉴바가 숨겨져 있으면 오작동하지 않음
- 이벤트 감지기가 중단되면 자동으로 다시 등록

### 전체화면

- 메뉴바가 완전히 숨겨져 있는 동안 빈 영역 클릭 기능은 동작하지 않음
- 메뉴바가 나타난 상태에서는 실제 빈 영역 클릭 가능
- 전역 단축키는 전체화면에서도 사용할 수 있음
- 단축키로 열 때 현재 화면의 안전 영역 아래에 Fire Bar 표시
- 전체화면 앱 전환으로 화면 좌표가 바뀌면 Fire Bar 닫기

---

## 20. Fire 자체 아이콘

Fire는 구역을 관리하기 위한 제어 아이템을 가진다.

### 동작

- 불꽃 아이콘 클릭: 설정창 열기
- 아이콘 자체에는 드롭다운 메뉴 없음
- 오른쪽 클릭 기능 없음
- 설정 화면에서 MAIN 또는 FIRE_BAR로 이동 가능
- Fire Bar 안에 있을 때도 일반 아이콘과 같은 크기로 표시
- Fire 아이콘을 찾지 못해도 단축키로 복구 가능

### 안전장치

Fire 아이콘이 Fire Bar에 있고 Fire Bar가 열리지 않는 문제가 생기더라도 다음으로 복구할 수 있다.

```text
⌥⌘F     Fire Bar 열기 시도
⌥⌘,     설정창 직접 열기
```

설정창의 `메뉴바 상태 다시 적용` 버튼으로 전체 구성을 다시 만든다.

---

## 21. 수동 복구 기능

기능은 최소화하되 재설치 없이 복구할 수 있는 버튼은 반드시 둔다.

### 아이콘 다시 검색

현재 실행 중인 메뉴바 앱을 다시 탐색한다.

사용 상황:

- 새 메뉴바 앱 설치
- 메뉴바 앱 실행 또는 종료
- 아이콘이 설정 화면에 나타나지 않음

### 메뉴바 상태 다시 적용

모든 런타임 참조를 버리고 전체 구성을 다시 만든다.

사용 상황:

- 외부 모니터 연결 후 배치 이상
- 잠자기 복귀 후 Fire Bar가 비어 있음
- 아이콘 클릭 불가
- Fire Bar 위치 이상
- 업데이트 후 항목 식별 실패

이 기능은 앱 삭제나 재설치를 대신하는 복구 수단이다.

---

## 22. 데이터 저장

`Application Support/Fire/`에 설정을 저장한다.

```text
Fire/
├── layout.json
├── settings.json
└── recovery.json
```

### `layout.json`

- 아이콘 stableId
- 소유 앱 정보
- MAIN / FIRE_BAR 구역
- 구역 내 순서
- 마지막 확인 시각

### `settings.json`

- 로그인 시 자동 실행 요청 상태
- 단축키
- 자동 닫기 시간
- 권한 안내 완료 여부

### `recovery.json`

- 마지막 정상 적용 상태
- 마지막 재구성 결과
- 연속 실패 횟수
- 최근 오류 코드

### 저장하지 않는 값

다음 값은 영구 저장하지 않는다.

- PID
- AXUIElement 객체
- NSWindow 객체
- 윈도우 번호
- 현재 디스플레이 번호
- 현재 화면 좌표
- 캡처된 아이콘 이미지 원본
- 화면 전체 이미지

---

## 23. 기술 구성

### 권장 기술

- Swift
- AppKit
- SwiftUI 일부
- ApplicationServices Accessibility API
- Core Graphics
- ScreenCaptureKit 또는 필요한 캡처 API
- ServiceManagement `SMAppService`
- `NSStatusItem`
- `NSPanel`
- 전역 이벤트 탭
- `NSWorkspace` 시스템 알림

### 주요 컴포넌트

```text
Fire/
├── App/
│   ├── FireApp.swift
│   ├── AppDelegate.swift
│   └── AppLifecycleController.swift
├── StatusItem/
│   ├── FireStatusItemController.swift
│   └── ControlItemCoordinator.swift
├── MenuBar/
│   ├── MenuBarScanner.swift
│   ├── MenuBarItem.swift
│   ├── MenuBarItemIdentity.swift
│   ├── MenuBarLayoutController.swift
│   └── MenuBarActionProxy.swift
├── FireBar/
│   ├── FireBarController.swift
│   ├── FireBarPanel.swift
│   ├── FireBarItemView.swift
│   └── FireBarPositioner.swift
├── Events/
│   ├── GlobalClickMonitor.swift
│   ├── EmptyMenuBarHitTester.swift
│   ├── HotkeyManager.swift
│   └── SystemEventMonitor.swift
├── Stability/
│   ├── RebuildCoordinator.swift
│   ├── RecoveryStateMachine.swift
│   ├── Watchdog.swift
│   └── PermissionHealthMonitor.swift
├── Settings/
│   ├── SettingsWindowController.swift
│   ├── LayoutEditorView.swift
│   ├── DraggableMenuBarItemView.swift
│   └── SettingsStore.swift
├── Permissions/
│   ├── AccessibilityPermissionManager.swift
│   └── ScreenCapturePermissionManager.swift
├── LoginItem/
│   └── LoginItemManager.swift
└── Resources/
    └── Assets.xcassets
```

---

## 24. 구현 전략

### 원칙

- Ice의 소스코드를 그대로 복사하지 않는다.
- 기능과 사용자 흐름만 참고해 새 구조로 구현한다.
- 기존 코드를 포크해서 사용한다면 GPL-3.0 조건을 준수해야 한다.
- 개인용 독립 앱은 clean-room 구현을 우선한다.
- Apple이 공식적으로 보장하지 않는 메뉴바 내부 동작은 별도 호환 계층으로 격리한다.
- macOS 버전별 코드 분기를 한곳에 모은다.
- 시스템 내부 동작이 바뀌어도 UI 전체를 다시 작성하지 않도록 한다.

### Private API 가능성

임의의 다른 앱 메뉴바 아이콘을 숨기고 순서를 강제로 관리하는 기능은 공식 공개 API만으로 완전하게 구현하기 어려울 수 있다.

따라서 다음 원칙을 둔다.

1. 공개 접근성 API로 가능한 범위를 먼저 구현
2. 부족한 기능만 최소한의 비공개 브리지로 격리
3. 비공개 기능 실패 시 앱 전체가 죽지 않도록 fallback
4. macOS 업데이트 후 호환성 검사를 가장 먼저 실행
5. App Store 배포보다 직접 서명·공증 배포를 우선 고려

---

## 25. Phase 0 기술 검증

전체 앱을 만들기 전에 다음 항목을 작은 실험 앱으로 검증한다.

### 검증 A — 메뉴바 항목 탐색

- 현재 메뉴바 아이콘 목록 조회
- 각 항목의 프레임 확인
- 소유 앱 식별
- 설정 화면에 실제 아이콘 표시
- 앱 재시작 후 같은 항목 매칭

### 검증 B — 아이콘 숨김

- 선택한 아이콘을 메인 메뉴바에서 숨김
- 다른 메뉴바 아이콘 클릭에 영향 없음
- 숨긴 앱을 재실행해도 상태 복원
- 시스템 아이콘과 제어 센터 아이콘 처리

### 검증 C — Fire Bar

- 숨긴 아이콘을 별도 패널에 표시
- Fire Bar 아이콘 클릭 시 원본 메뉴 열기
- 메뉴가 화면 밖에 나타나지 않음
- 바깥 클릭으로 닫기
- 5초 자동 닫기
- 드래그 순서 변경

### 검증 D — 빈 영역 클릭

- 오른쪽 빈 공간 클릭
- 왼쪽 빈 공간 클릭
- Apple 메뉴 클릭 방해 없음
- 앱 메뉴 클릭 방해 없음
- 메뉴바 아이콘 클릭 방해 없음
- 노치 주변 좌표 처리

### 검증 E — 안정성

- 외부 모니터 10회 연결·해제
- 잠자기·복귀 10회
- 주 디스플레이 변경
- 해상도 변경
- 클램셸 모드
- 메뉴바 앱 종료·재실행

### Phase 0 통과 기준

다음이 모두 가능할 때 본 개발로 진행한다.

1. 숨긴 아이콘을 Fire Bar에 표시 가능
2. Fire Bar 아이콘 클릭이 실제 앱 동작으로 연결
3. 왼쪽·오른쪽 빈 메뉴바 클릭이 다른 메뉴를 방해하지 않음
4. 외부 모니터 재연결 후 자동 복구
5. 잠자기 복귀 후 자동 복구
6. 앱 재설치 없이 수동 재구성으로 복구 가능

---

## 26. 개발 단계

### Phase 1 — 앱 뼈대

- macOS accessory 앱
- Dock 비표시
- Fire 불꽃 상태 아이템
- 설정창
- 로그인 시 자동 실행
- 기본 단축키

### Phase 2 — 메뉴바 탐색

- 권한 안내
- 메뉴바 항목 수집
- 안정적 식별자
- 아이콘 이미지 표시
- 설정 화면 목록

### Phase 3 — 두 구역 관리

- MAIN / FIRE_BAR 데이터 모델
- 설정 화면 드래그 앤 드롭
- 구역 내 순서 변경
- 실제 메뉴바 숨김 및 복원

### Phase 4 — Fire Bar

- 별도 패널
- 클릭 위치 기준 표시
- 아이콘 클릭 프록시
- 바깥 클릭 및 `Esc`
- 5초 자동 닫기
- 단축키 토글

### Phase 5 — 빈 메뉴바 클릭

- 왼쪽 빈 영역 판정
- 오른쪽 빈 영역 판정
- 노치와 앱 메뉴 충돌 방지
- 다중 모니터 좌표 처리

### Phase 6 — 안정성

- 디스플레이 이벤트
- 잠자기 복귀
- 재구성 상태 머신
- 지연 재시도
- Watchdog
- 수동 복구

### Phase 7 — 실사용 검증

- 사용자 실제 메뉴바 앱 조합
- 장시간 실행
- 외부 모니터 반복 연결
- 전체화면
- 클램셸
- 시스템 업데이트 전후 검증

### Phase 8 — 패키징

- Developer ID 서명
- 공증
- DMG
- 개인 배포
- 필요한 경우 업데이트 방식 검토

---

## 27. 테스트 체크리스트

### 기본

- Dock에 Fire가 나타나지 않는다.
- Fire 아이콘을 누르면 설정창이 열린다.
- 설정창을 여러 번 열어도 중복 창이 생기지 않는다.
- `⌥⌘F`가 모든 앱에서 동작한다.
- `⌥⌘,`로 설정창이 열린다.
- 로그인 후 자동 실행된다.

### 빈 영역 클릭

- 오른쪽 빈 공간 클릭으로 열린다.
- 왼쪽 빈 공간 클릭으로 열린다.
- Apple 메뉴가 정상 동작한다.
- 현재 앱 메뉴가 정상 동작한다.
- 시스템 아이콘이 정상 동작한다.
- 노치 부분 클릭으로 오작동하지 않는다.
- 빈 공간이 없는 상태에서 다른 항목 클릭을 훔치지 않는다.

### Fire Bar

- 클릭한 모니터에 표시된다.
- 클릭 지점 아래에 표시된다.
- 화면 경계를 넘지 않는다.
- 바깥 클릭 시 닫힌다.
- `Esc`로 닫힌다.
- 5초 동안 사용하지 않으면 닫힌다.
- 마우스가 위에 있으면 닫히지 않는다.
- 아이콘 메뉴가 열려 있으면 닫히지 않는다.
- 아이콘 클릭이 원본 앱에 전달된다.

### 설정

- 아이콘을 MAIN에서 FIRE_BAR로 이동할 수 있다.
- 아이콘을 FIRE_BAR에서 MAIN으로 이동할 수 있다.
- 각 구역 내 순서를 바꿀 수 있다.
- Fire 아이콘도 이동할 수 있다.
- 앱을 재시작해도 배치가 유지된다.
- 새 아이콘이 자동으로 발견된다.
- 사라진 아이콘이 설정에 영구 유령 항목으로 남지 않는다.

### 외부 모니터

- 앱 실행 전 외부 모니터 연결
- 앱 실행 후 외부 모니터 연결
- 외부 모니터 분리
- 다른 포트로 재연결
- 허브 재연결
- 주 모니터 변경
- 화면 배열 변경
- 해상도 변경
- 배율 변경
- 미러링 전환
- 클램셸 진입 및 해제
- Fire Bar가 열린 화면을 분리

### 잠자기

- 화면 절전 후 복귀
- Mac 잠자기 후 복귀
- 잠자기 중 모니터 연결
- 잠자기 중 모니터 분리
- 잠자기 전 Fire Bar가 열린 상태
- 사용자 잠금 후 재로그인
- 장시간 잠자기 후 복귀

### 오류 복구

- 손쉬운 사용 권한 해제
- 화면 기록 권한 해제
- 메뉴바 앱 강제 종료
- 메뉴바 앱 재실행
- Fire 이벤트 탭 중단
- 설정의 `아이콘 다시 검색`
- 설정의 `메뉴바 상태 다시 적용`
- Fire 강제 종료 후 다시 실행

---

## 28. 완료 기준

1. 메인 메뉴바와 Fire Bar 두 구역만 존재한다.
2. 설정 화면에서 아이콘을 드래그해 구역과 순서를 바꿀 수 있다.
3. Fire 자체 아이콘도 두 구역 사이를 이동할 수 있다.
4. 메뉴바 왼쪽과 오른쪽의 실제 빈 공간 클릭으로 Fire Bar가 열린다.
5. Apple 메뉴, 앱 메뉴, 시스템 아이콘 클릭을 방해하지 않는다.
6. Fire Bar는 클릭한 모니터와 클릭 위치를 기준으로 표시된다.
7. Fire Bar 바깥 클릭 시 닫힌다.
8. `Esc`로 닫힌다.
9. 5초 동안 사용하지 않으면 자동으로 닫힌다.
10. `⌥⌘F`로 Fire Bar를 열고 닫을 수 있다.
11. `⌥⌘,`로 설정창을 열 수 있다.
12. Fire Bar 아이콘 클릭이 실제 메뉴바 앱 동작으로 연결된다.
13. 로그인 시 자동 실행된다.
14. 외부 모니터를 20회 반복 연결·해제해도 재설치가 필요하지 않다.
15. 잠자기·복귀를 10회 반복해도 자동 복구된다.
16. 주 디스플레이, 해상도, 화면 배열 변경 후 5초 안에 복구된다.
17. 메뉴바 앱이 재실행되면 기존 분류와 순서를 가능한 범위에서 복원한다.
18. 상태가 꼬여도 설정의 재적용 버튼으로 복구된다.
19. 평상시 CPU 사용량이 낮다.
20. 화면 데이터와 아이콘 이미지를 외부로 전송하지 않는다.

---

## 29. 예상 난이도와 기간

화면과 기능 수는 적지만 macOS의 다른 앱 메뉴바 항목을 식별하고 이동하는 부분은 시스템 통합 난이도가 높다.

### 예상 일정

| 단계 | 예상 소요 |
|---|---:|
| Phase 0 기술 검증 | 2~5일 |
| 앱 뼈대와 설정 화면 | 1~2일 |
| 메뉴바 탐색과 구역 관리 | 3~5일 |
| Fire Bar 및 클릭 프록시 | 3~5일 |
| 빈 영역 클릭 | 2~3일 |
| 외부 모니터·잠자기 복구 | 3~5일 |
| 장시간 테스트와 안정화 | 5~10일 |
| 서명·공증·패키징 | 1~2일 |

기본 MVP는 약 1주, 실제로 안정적인 개인용 버전은 약 2~4주를 예상한다.

가장 중요한 것은 전체 UI를 먼저 만드는 것이 아니라 Phase 0에서 다음 세 가지를 먼저 증명하는 것이다.

- 숨긴 아이콘을 Fire Bar에서 실제로 클릭할 수 있는가
- 왼쪽과 오른쪽 빈 메뉴바 클릭을 안전하게 구분할 수 있는가
- 외부 모니터와 잠자기 후 자동 재구성이 가능한가

---

## 30. 최종 제품 정의

Fire는 Ice의 모든 기능을 복제하는 앱이 아니다.

다음만 확실하게 수행한다.

```text
메뉴바 아이콘 탐색
→ 메인 메뉴바 / Fire Bar 분류
→ 드래그로 순서 변경
→ 빈 공간 클릭 또는 단축키로 Fire Bar 열기
→ 실제 아이콘 사용
→ 외부 모니터·잠자기 후 자동 복구
```

기능을 줄이는 이유는 설정을 단순하게 만드는 것뿐 아니라, macOS 업데이트와 디스플레이 변경에 대응해야 할 상태 수를 줄여 안정성을 높이기 위해서다.

Fire의 성공 기준은 사용자가 앱을 의식하지 않는 것이다.

> 한 번 아이콘을 배치하면 이후 외부 모니터를 연결하거나 Mac을 재웠다 깨워도 그대로 작동해야 한다.
